$('.anyslider').anythingSlider({
    resizeContents: true, // Resize contents to fit the container
    buildNavigation: false, // No navigation
    buildStartStop: false, // No start stop button
    autoPlay: true, // will cycle automatically
    startPanel: 1, // start at 1
    pauseOnHover: true, // when the user hovers on a slide, pause the carousel
    delay: 5000, // time to display each slide - 5 seconds per slide
});
